#include "Shape.h"
#include "Constants.h"

Shape::Shape(Vec3 surfaceColor, double transparency) : surfaceColor(surfaceColor), transparency(transparency) {
}

double Shape::intersectionPoint(const Vec3 &rayOrigin, const Vec3 &rayDirection) {

}